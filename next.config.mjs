/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    assetPrefix: './',
    basePath: '/mon-blog4',
    images: {
      unoptimized: true,
    },
  };
  export default nextConfig;
  